import { SelectDocumentDirective } from './select-document.directive';

describe('SelectDocumentDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectDocumentDirective();
    expect(directive).toBeTruthy();
  });
});
